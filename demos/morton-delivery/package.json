{
  "name": "morton-delivery-demo",
  "private": true,
  "type": "module",
  "dependencies": {
    "remix": "workspace:*"
  },
  "devDependencies": {
    "@types/dom-navigation": "^1.0.7",
    "@types/node": "catalog:",
    "@typescript/native-preview": "catalog:",
    "tsx": "catalog:"
  },
  "scripts": {
    "dev": "NODE_ENV=development tsx watch server.ts",
    "start": "tsx server.ts",
    "build": "tsx build.ts",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit"
  }
}
